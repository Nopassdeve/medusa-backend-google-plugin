/**
 * Admin extension entry point for medusa-backend-google-plugin
 * 
 * Exports routes and widgets that will be auto-discovered by Medusa.
 * (Medusa scans this directory directly as a source, no manual plugin object needed)
 */
